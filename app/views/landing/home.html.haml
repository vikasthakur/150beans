%section.home
  %h1 User Home
  
  - current_user.groups.each do |g|
    %section.group
      %section.transactions.grid_6.alpha
        %h1= "#{g.name} transactions"
        = render :partial => 'transactions/form', :locals => { :journal => g.journal }
        = render :partial => 'transactions/list', :locals => { :transactions => Transaction.for_journal(g.journal).rev_chrono }
    
      %section.sidebar.grid_10
        %h1 spending
      .clear